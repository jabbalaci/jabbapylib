Third-party libraries.

* BeautifulSoup
  version 3.2.0
* html2text
  Convert HTML to ascii text.
* markov_passwords
  Generates readable passwords. Includes no digits, so 
  I use it to generate readable usernames.
* soupselect
  Monkeypatch for BeautifulSoup to provide CSS selection. 
* termcolor
  Print colored text in Linux terminals.
 